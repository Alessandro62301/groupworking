import type { ReactNode } from 'react';

import { AdminShell } from '@/components/layout/admin-shell';

const ADMIN_NAV_ITEMS = [
  { label: 'Intenções', href: '/admin/intentions', badge: '2' },
  { label: 'Dashboard', href: '/admin/dashboard' },
  { label: 'Avisos', href: '/admin/announcements' },
  { label: 'Mensalidades', href: '/admin/dues' },
];

export default function AdminLayout({ children }: { children: ReactNode }) {
  return (
    <AdminShell
      navItems={ADMIN_NAV_ITEMS}
      headerTitle="Painel do administrador"
      headerSubtitle="Gerencie intenções, avisos, indicadores e mensalidades do grupo."
    >
      {children}
    </AdminShell>
  );
}
